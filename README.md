Download the dist folder and run ConsoleApp.exe, which can be found in that same folder.

The code can be found in folder ConsoleApp, file DirectoryManager.cs, and was developed in Visual Studio using C#.

Executable built via the following command: `dotnet publish -c Release -r win-x64 --self-contained`